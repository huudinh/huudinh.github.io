let task = [];
let html = {
    name: "HTML",
    complete: false,
};
let css = {
    name: "CSS",
    complete: false,
};
let js = {
    name: "Basic of JavaScript",
    complete: false,
};
let nodejs = {
    name: "Node Package Manager (npm)",
    complete: false,
}
let git = {
    name: "Git",
    complete: false,
};
task.push(html);
task.push(css);
task.push(js);
task.push(nodejs);
task.push(git);